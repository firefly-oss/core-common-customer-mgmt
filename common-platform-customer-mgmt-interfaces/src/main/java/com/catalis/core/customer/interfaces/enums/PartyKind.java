package com.catalis.core.customer.interfaces.enums;

/**
 * Enum representing the kind of party (Individual or Organization).
 * Maps to the PostgreSQL party_kind_enum.
 */
public enum PartyKind {
    INDIVIDUAL,
    ORGANIZATION
}