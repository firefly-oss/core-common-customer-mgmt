package com.catalis.core.customer.interfaces.enums;

/**
 * Enum representing address kind options for addresses.
 * Maps to the PostgreSQL address_kind_enum.
 */
public enum AddressKind {
    HOME,
    WORK,
    MAILING,
    REGISTERED
}