package com.catalis.core.customer.interfaces.enums;

/**
 * Enum representing phone kind options for phone contacts.
 * Maps to the PostgreSQL phone_kind_enum.
 */
public enum PhoneKind {
    MOBILE,
    HOME,
    WORK,
    OTHER
}