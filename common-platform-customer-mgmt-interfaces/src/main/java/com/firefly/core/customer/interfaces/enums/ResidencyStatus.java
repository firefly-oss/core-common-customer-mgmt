package com.firefly.core.customer.interfaces.enums;

/**
 * Enum representing residency status options for natural persons.
 * Maps to the PostgreSQL residency_status_enum.
 */
public enum ResidencyStatus {
    RESIDENT,
    NON_RESIDENT
}